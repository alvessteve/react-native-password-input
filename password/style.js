import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  div: {
    marginTop: 20,
    flexDirection: "row"
  },
  input: {
    fontSize: 18,
    width: "100%"
  },
  icon: {
    marginLeft: "auto"
  }
});
